const jsConfetti = new JSConfetti();

jsConfetti.addConfetti({
    emojis: ['💚'],
    emojiSize: 60,
    confettiNumber: 30,
});

// jsConfetti.addConfetti({
//     emojis: ['❌'],
//     emojiSize: 60,
//     confettiNumber: 35,
// });